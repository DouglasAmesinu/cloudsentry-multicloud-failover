services:
  app:
    build: .
    environment:
      - PROVIDER=${PROVIDER}
      - REGION=${REGION}
    restart: unless-stopped

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - app
    restart: unless-stopped
